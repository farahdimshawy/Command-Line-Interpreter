import java.util.Scanner;
import java.io.*;

public class Main {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        //String directory = scanner.nextLine();
        //CLI.rmdir(new File(directory));
        System.out.println(CLI.pwd());
    }
}